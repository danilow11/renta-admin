info:
  name: Get Property by ID
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/properties/{{propertyId}}"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
